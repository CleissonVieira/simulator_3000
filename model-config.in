# Nesse arquivo estarão definidos o fluxo e componentes do modelo. A cada criação de componente, sera criado uma coluna na matriz de calculo

# O usuario irá utilizar o padrão estabelecido para os componentes, que é sempre iniciar pela tag de 3 letras identificando qual componete ele está criando
# Os dois primeiros componentes podem ser criados apenas uma vez, sendo o tempo de simulação e componente gerador de entidades temporarias, respectivamente.

# Tag Tempo da simulação ; Tempo da simulação em min
STM ; 40

# Tag Entidade Temporaria ; identificador ; controle tempo (contador) ; tempo gasto ; localização ; status
ETP ; id ; ct ; tg ; loc ; status

# Tag Gerador de Entidade Temporaria ; nome componente onde irá iniciar  ; intervalo tempo de operação ; intervalo entre geração
SRC ; gerador ; entradaPrincipal ; (1,20) ; 2 

# Tag roteador ; nome roteador ; origem roteador (SRC) ; saídas(probabilidade) roteador
ROT ; entradaPrincipal ; gerador ; filaEstacao1(0.7),entradaTrem1(0.3)

# Tag fila ; nome fila ; origem ; destino ; algoritimo de seleção (F=FCFS, P=prioridade, L=LCFS)
QUE ; filaEstacao1 ; entradaPrincipal ; estacao1 ; F 

# Tag comp finito (com fila) ; nome comp finito ; origem ; destino ; nº atendentes ; intervalo gasto
GSF ; estacao1 ; filaEstacao1 ; entradaTrem1 ; 2 ; (2,7) 

# Tag comp infitio (sem fila) ; nome ; origem ; destino ; intervalo gasto
GSI ; estacao2 ; filaEstacao1 ; entradaTrem2 ; (1,5) 

# Tag comp saída ; nome ; origem (quando entra o usuario morre)
OUT ; entradaTrem1 ; estacao1
OUT ; entradaTrem2 ; estacao2
